services:
  popo_blog:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: popo_blog
    ports:
      - "8000:8000"
      - "9000:9000"
    volumes:
      - .:/app
    working_dir: /app
    stdin_open: true
    environment:
      - NODE_ENV=development